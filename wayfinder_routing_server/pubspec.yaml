name: wayfinder_routing_server
description: Wayfinder routing appliance wrapping GraphHopper for OSM A→B routing.
publish_to: none

environment:
  sdk: '^3.8.0'

dependencies:
  args: ^2.7.0
  http: ^1.2.2
  path: ^1.9.1
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  test: ^1.25.5
