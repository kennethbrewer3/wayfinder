name: wayfinder_geocoding_server
description: Wayfinder geocoding Serverpod server (OSMNames import and search).
publish_to: none

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  crypto: ^3.0.6
  serverpod: 3.4.8

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.8
  test: ^1.25.5

serverpod:
  scripts:
    start: dart bin/main.dart --apply-migrations
