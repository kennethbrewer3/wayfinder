class: PmtilesGroup
table: pmtiles_group
fields:
  id: UuidValue, defaultModel=random
  name: String
  sortOrder: int, defaultModel=0
  createdAt: DateTime
  showOnMap: bool, defaultModel=false, defaultPersist=false

indexes:
  pmtiles_group_name_idx:
    fields: name
