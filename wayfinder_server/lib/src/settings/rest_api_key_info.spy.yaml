class: RestApiKeyInfo
fields:
  enabled: bool
  ### True when WAYFINDER_REST_API_KEY is set in the server environment.
  envKeyConfigured: bool
  ### Plaintext key; only returned immediately after create.
  apiKey: String?
